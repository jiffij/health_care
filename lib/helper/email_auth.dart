import 'package:flutter/material.dart';

class waitingEmailAuth extends StatelessWidget {
  const waitingEmailAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}